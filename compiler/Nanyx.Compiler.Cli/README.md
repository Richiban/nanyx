# NanyxCompiler.Cli

Tiny C# CLI runner for the Nanyx compiler pipeline, implemented with Consolo.

## Usage

Provide a `.nyx` file path and the CLI prints the compile phase, diagnostics, and inferred types.

The command-line entrypoint and help text are generated by Consolo from the command method in Program.cs.
